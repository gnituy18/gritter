version: "3"

services:
  backend:
    build: ../backend
    ports:
      - "8080:8080"
    depends_on:
      - mongo
  mongo:
    image: mongo:4.4
    restart: always
    ports: 
      - "27017:27017"
