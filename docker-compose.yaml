version: "3"

services:
  backend:
    build: 
      dockerfile: ./backend/Dockerfile
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - mongo
  mongo:
    image: mongo:4.4
    restart: always
    ports: 
      - "27017:27017"
